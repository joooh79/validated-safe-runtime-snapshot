{
  "name": "smr-sender-rebuild-clean-package",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:test": "tsc -p tsconfig.test.json",
    "build:validation": "tsc -p tsconfig.validation.json",
    "prevalidate:golden": "npm run build:validation",
    "validate:golden": "node .validation-build/src/validation/cli.js",
    "preapi:examples": "npm run build:validation",
    "api:examples": "node .validation-build/src/api/__fixtures__/cli.js",
    "pretest": "npm run build:test",
    "test": "node --test .test-build/test/server.test.js .test-build/test/auth/verifyProxyRequest.test.js .test-build/test/api/buildReadablePreview.test.js .test-build/test/api/orchestrateRequest.test.js .test-build/test/ui/controller.test.js",
    "start": "node dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
